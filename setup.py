"""
Install script for the package, to make pip install possible and support proper imports.
"""

from setuptools import setup, find_packages

setup(name="interpretability-comparison", version="0.1", packages=find_packages())
